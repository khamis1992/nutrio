---
phase: 01-translation-keys
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: ["src/contexts/LanguageContext.tsx", "src/pages/Profile.tsx"]
autonomous: true
requirements: ["TRANS-01"]
user_setup: []
must_haves:
  truths:
    - "All 10 translation keys for Profile page exist in both en and ar dictionaries"
    - "Profile.tsx uses t() function for all toast message strings"
    - "No hardcoded English strings remain in Profile.tsx toast messages"
  artifacts:
    - path: "src/contexts/LanguageContext.tsx"
      provides: "Translation dictionary with Profile page keys"
      contains: "profile_updated, password_too_short, dietary_preference_added"
    - path: "src/pages/Profile.tsx"
      provides: "Profile page using translation keys"
      exports: "Profile component with t() references"
  key_links:
    - from: "src/pages/Profile.tsx"
      to: "src/contexts/LanguageContext.tsx"
      via: "translation key usage via t() function"
      pattern: 't\\("profile_updated"\\)|t\\("password_too_short"\\)|t\\("dietary_preference_added"\\)'
---

<objective>
Add missing translation keys for Profile page error/success messages.

Purpose: Profile.tsx contains hardcoded English strings in toast notifications that should use translation keys to support Arabic localization. These messages include errors, success notifications, and validation feedback for profile updates, password changes, and dietary preference toggles.

Output: Complete translation dictionary with Profile-specific error/success keys in both English and Arabic, and Profile.tsx updated to use these translation keys.
</objective>

<execution_context>
@C:/Users/khamis/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/khamis/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/contexts/LanguageContext.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Profile translation keys section to LanguageContext.tsx</name>
  <files>src/contexts/LanguageContext.tsx</files>
  <action>
    Read LanguageContext.tsx and add the following translation keys to both `en` and `ar` dictionaries under the "Profile page extensions" section (at line 607). First verify the section header position via grep:
    - Run: `grep -n 'Profile page extensions' src/contexts/LanguageContext.tsx` to confirm exact line number
    - Then add keys at that section header position

    English keys (en dictionary):
    - `profile_updated`: "Profile updated"
    - `profile_updated_description`: "Your personal information has been saved."
    - `password_too_short`: "Password too short"
    - `failed_load_dietary_preferences`: "Failed to load dietary preferences"
    - `failed_update_dietary_preference`: "Failed to update dietary preference"
    - `dietary_preference_removed`: "Dietary preference removed"
    - `dietary_preference_added`: "Dietary preference added"
    - `error_updating_password`: "Error updating password"
    - `dietary_preference_removed_description`: "has been removed successfully"
    - `dietary_preference_added_description`: "has been added successfully"

    Arabic keys (ar dictionary):
    - `profile_updated`: "تم تحديث الملف الشخصي"
    - `profile_updated_description`: "تم حفظ معلوماتك الشخصية."
    - `password_too_short`: "كلمة المرور قصيرة جداً"
    - `failed_load_dietary_preferences`: "فشل تحميل التفضيلات الغذائية"
    - `failed_update_dietary_preference`: "فشل تحديث التفضيل الغذائي"
    - `dietary_preference_removed`: "تمت إزالة التفضيل الغذائي"
    - `dietary_preference_added`: "تمت إضافة التفضيل الغذائي"
    - `error_updating_password`: "خطأ في تحديث كلمة المرور"
    - `dietary_preference_removed_description`: "تمت إزالته بنجاح"
    - `dietary_preference_added_description`: "تمت إضافته بنجاح"

    Place keys alphabetically within the section after existing Profile page extensions keys.
    Ensure no duplicate keys exist in the file.
  </action>
  <verify>
    Run: `npm run typecheck 2>&1 | head -20` - should show no errors in LanguageContext.tsx
    Run: `grep -c 'profile_updated' src/contexts/LanguageContext.tsx` should return 2 (en + ar dictionaries)
    Run: `grep -E 'profile_updated|profile_updated_description|password_too_short|failed_load_dietary_preferences|failed_update_dietary_preference|dietary_preference_removed|dietary_preference_added|error_updating_password|dietary_preference_removed_description|dietary_preference_added_description' src/contexts/LanguageContext.tsx | head -20` should show all 10 keys in both languages
  </verify>
  <done>
    - LanguageContext.tsx has no TypeScript errors
    - All 10 new translation keys exist in both `en` and `ar` dictionaries
    - No duplicate keys found
    - Keys are properly grouped under "Profile page extensions" section
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace hardcoded English strings in Profile.tsx with translation keys</name>
  <files>src/pages/Profile.tsx</files>
  <action>
    Replace all hardcoded English strings in Profile.tsx toast messages with translation key references using the `t()` function:

    - Line 299: `"Error"` → `t("error")`, `"Failed to load dietary preferences"` → `t("failed_load_dietary_preferences")`
    - Line 324: `"Removed"` → `t("removed")`, `"Added"` → `t("added")`, `"removed"` → `t("dietary_preference_removed")`, `"added"` → `t("dietary_preference_added")`
    - Line 326: `"Error"` → `t("error")`, `"Failed to update dietary preference"` → `t("failed_update_dietary_preference")`
    - Line 384: `"Profile updated"` → `t("profile_updated")`, `"Your personal information has been saved."` → `t("profile_updated_description")`
    - Line 390: `"Please try again."` → `t("please_try_again")`
    - Line 401: `"Passwords don't match"` → `t("passwords_dont_match")`
    - Line 410: `"Password too short"` → `t("password_too_short")`
    - Line 411: `"Password must be at least 6 characters."` → `t("password_min_length")`
    - Line 429: `"Password updated"` → `t("password_updated")`, `"Your password has been changed successfully."` → `t("password_changed_success")`
    - Line 435: `"Please try again."` → `t("please_try_again")`
    - Line 450: `"Contact support"` → `t("contact_support")`
    - Line 451: `"Please contact support to delete your account."` → `t("contact_support_delete_account")`

    Run `npm run typecheck` to verify no TypeScript errors.
  </action>
  <verify>
    Run: `npm run typecheck 2>&1 | head -30` - should show no errors in Profile.tsx
    Also verify: `grep -c 't("' src/pages/Profile.tsx` should be > 0 (translation keys are being used)
    Cross-reference verification: `grep -o 't("[^"]*")' src/pages/Profile.tsx | sed 's/t("//g' | sed 's/")//g' | while read key; do grep -q "$key" src/contexts/LanguageContext.tsx && echo "OK: $key" || echo "MISSING: $key"; done` should show all keys exist in LanguageContext.tsx
  </verify>
  <done>
    - No hardcoded English strings remain in Profile.tsx toast messages
    - All toast messages use `t()` function with valid translation keys
    - No TypeScript errors in Profile.tsx
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify translation coverage and consistency</name>
  <files>src/contexts/LanguageContext.tsx, src/pages/Profile.tsx</files>
  <action>
    1. Verify all translation keys used in Profile.tsx exist in LanguageContext.tsx:
       - Run: `grep -o 't("[^"]*")' src/pages/Profile.tsx | sort -u` to list all translation keys used
       - For each key, verify it exists in both `en` and `ar` dictionaries with: `grep -E "\"$key\":" src/contexts/LanguageContext.tsx | wc -l` should return 2

    2. Run linter to verify code style:
       - `npm run lint -- src/pages/Profile.tsx src/contexts/LanguageContext.tsx`

    3. Build the project to verify no compilation errors:
       - `npm run build 2>&1 | head -30`

    4. Final verification: Search Profile.tsx for hardcoded strings:
       - `grep -nE '"Error"|"Removed"|"Added"|"Profile updated"|"Passwords don'\''t match"|"Password too short"|"Please try again"' src/pages/Profile.tsx`
       - Should return no matches (all strings should be wrapped in `t()` function)
       - Also verify: `grep -n 'toast\|notification' src/pages/Profile.tsx | grep -v 't(' | head -10` should show no toast notifications with hardcoded strings
  </action>
  <verify>
    Run: `npm run lint -- src/pages/Profile.tsx src/contexts/LanguageContext.tsx 2>&1 | tail -20`
    Run: `npm run build 2>&1 | head -30`
    Cross-reference verification: `grep -o 't("[^"]*")' src/pages/Profile.tsx | sed 's/t("//g' | sed 's/")//g' | while read key; do grep -q "$key" src/contexts/LanguageContext.tsx && echo "OK: $key" || echo "MISSING: $key"; done`
  </verify>
  <done>
    - All Profile.tsx translation keys exist in LanguageContext.tsx
    - No linting errors in modified files
    - Build succeeds without errors for modified files
    - No hardcoded English strings found in Profile.tsx
    - All success criteria from ROADMAP.md Phase 1 met:
      1. All required translation keys exist in `en` dictionary
      2. Each key has corresponding Arabic translation
      3. Keys are organized under "Profile page extensions" section
      4. No duplicate keys exist
  </done>
</task>

</tasks>

<verification>
1. `npm run typecheck` passes without errors on modified files
2. `npm run lint` passes without errors on modified files
3. `npm run build` succeeds without errors
4. No hardcoded English strings remain in Profile.tsx toast messages
5. All translation keys in Profile.tsx have corresponding entries in LanguageContext.tsx
6. All new keys have translations in both `en` and `ar` dictionaries
</verification>

<success_criteria>
1. All required translation keys for Profile page error/success messages exist in the `en` translation dictionary of LanguageContext.tsx
2. Each new key has a corresponding Arabic translation in the `ar` dictionary
3. Translation keys are properly organized under "Profile page extensions" section of LanguageContext.tsx
4. No duplicate translation keys exist
5. Profile.tsx no longer contains hardcoded English strings for toast messages
6. All translation keys used in Profile.tsx exist in LanguageContext.tsx
7. `npm run typecheck`, `npm run lint`, and `npm run build` all pass without errors
</success_criteria>

<output>
After completion, run:
```bash
node "$HOME/.config/opencode/get-shit-done/bin/gsd-tools.cjs" commit "feat(01): add Profile translation keys" --files src/contexts/LanguageContext.tsx src/pages/Profile.tsx
```

Then create `.planning/phases/01-translation-keys/01-01-SUMMARY.md` with build output, file changes, and verification results.
</output>
