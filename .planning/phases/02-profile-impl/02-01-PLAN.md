---
phase: 02-profile-impl
plan: 01
type: execute
wave: 1
depends_on:
  - 01-01
files_modified: []
autonomous: true
requirements:
  - TRANS-02
user_setup: []
must_haves:
  truths:
    - "Profile page displays correctly in English with all text translated"
    - "No hardcoded English strings visible to users in Profile.tsx"
    - "All t() function calls have corresponding keys in LanguageContext.tsx"
  artifacts:
    - path: "src/pages/Profile.tsx"
      provides: "Profile page component with translation keys"
      contains: "t("
      min_lines: 50
    - path: "src/contexts/LanguageContext.tsx"
      provides: "English and Arabic translation dictionaries"
      contains: "profile_page_extensions"
  key_links:
    - from: "src/pages/Profile.tsx"
      to: "src/contexts/LanguageContext.tsx"
      via: "t() function calls"
      pattern: "t\\(\"[a-zA-Z_]+\"\\)"
---

# Phase 02 Plan 01: Profile Implementation Verification

**Verify no hardcoded English strings remain in Profile.tsx after Phase 1 translation key implementation**

## Purpose

This plan verifies that Phase 1 successfully integrated all translation keys and Profile.tsx contains no remaining hardcoded English strings. Based on analysis of the codebase, Profile.tsx already uses `t()` function calls throughout for all user-facing text, suggesting Phase 2 may already be complete as part of Phase 1's implementation.

## Output

Verification report documenting:
- All translation keys used in Profile.tsx
- Correspondence with LanguageContext.tsx translation dictionary
- Confirmation no hardcoded English strings remain

---

## Execution Context

@C:/Users/khamis/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/khamis/.config/opencode/get-shit-done/templates/summary.md

## Context

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/01-translation-keys/01-01-SUMMARY.md

@src/contexts/LanguageContext.tsx
@src/pages/Profile.tsx

---

## Tasks

<task type="auto">
  <name>Task 1: Verify all t() call keys exist in LanguageContext.tsx</name>
  <files>src/pages/Profile.tsx, src/contexts/LanguageContext.tsx</files>
  <action>
Extract all translation keys used in Profile.tsx and verify they exist in both English and Arabic dictionaries in LanguageContext.tsx:

1. Parse Profile.tsx to extract all `t("key_name")` calls
2. For each key, verify it exists in LanguageContext.tsx translations.en and translations.ar dictionaries
3. Document any missing keys (should be none if Phase 1 completed)
4. Confirm all Profile page sections are covered: personal_info, delivery_addresses, dietary_and_allergies, policies, support, wallet, rewards, settings

Return a report listing:
- Total t() calls found in Profile.tsx
- Keys verified in LanguageContext.tsx
- Any missing keys (if found)
- Section coverage map
  </action>
  <verify>
Automated check - no missing keys found in verification report
  </verify>
  <done>All translation keys used in Profile.tsx exist in LanguageContext.tsx en/ar dictionaries</done>
</task>

<task type="auto">
  <name>Task 2: Search for hardcoded English strings in Profile.tsx</name>
  <files>src/pages/Profile.tsx</files>
  <action>
Perform a comprehensive search for hardcoded English strings that should be using translation keys:

1. Search for common English phrases not wrapped in t() calls:
   - "Profile", "Save", "Cancel", "Loading", "Error", "Success"
   - "Personal info", "Delivery addresses", "Dietary and allergies"
   - "Support", "Wallet", "Rewards", "Settings"
   - "Male", "Female", "Year", "Years", "Email address"
   - "Phone number", "WhatsApp", "Email", "Call"
   
2. Check for English text in JSX not using t():
   - Button labels
   - Form placeholders
   - Accordion headers
   - Card titles
   - Toast messages

3. Search for English in strings that aren't translation keys:
   - Static text in JSX like `<span>Profile</span>`
   - Hardcoded values not wrapped in t()

Return a report listing any found hardcoded strings and their line numbers
  </action>
  <verify>
No hardcoded English strings found in search report, or any found strings are part of translation keys
  </verify>
  <done>No hardcoded English strings remain in Profile.tsx - all text uses translation keys</done>
</task>

<task type="auto">
  <name>Task 3: Verify Profile.tsx compiles and runs without errors</name>
  <files>src/pages/Profile.tsx</files>
  <action>
1. Run TypeScript type checking: `npm run typecheck` (filter for Profile.tsx if possible)
2. Attempt build: `npm run build` or `npm run build:dev`
3. Verify no errors related to missing translation keys
4. Check for any runtime issues that might indicate translation key problems

Return build/typecheck results
  </action>
  <verify>
npm run typecheck passes with zero errors for Profile.tsx
  </verify>
  <done>Profile.tsx compiles without errors, no missing translation key references</done>
</task>

---

## Verification

- [ ] All t() calls in Profile.tsx have keys in LanguageContext.tsx
- [ ] No hardcoded English strings found in Profile.tsx search
- [ ] Profile.tsx typechecks and builds without errors
- [ ] Phase 1 translation keys properly cover all Profile page sections

## Success Criteria

1. Profile.tsx uses translation keys for 100% of user-facing text
2. All translation keys exist in LanguageContext.tsx English dictionary
3. All translation keys have corresponding Arabic translations
4. No console warnings about missing translation keys
5. Profile page renders correctly in English language selection

---

## Output

After completion, create `.planning/phases/02-profile-impl/02-01-SUMMARY.md` documenting:
- Translation keys used in Profile.tsx
- Verification results
-confirmation no hardcoded strings remain
- whether Phase 2 is complete or additional work needed
