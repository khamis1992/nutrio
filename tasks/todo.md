# Step Goal Feature

## Plan

- [ ] 1. Add a `goalSteps` state stored in localStorage (per user), defaulting to 6000
- [ ] 2. Replace the hardcoded `STEP_GOAL` constant with the dynamic `goalSteps` state
- [ ] 3. Add a "Set Goal" row with preset chip options (3000, 5000, 6000, 8000, 10000, 15000) below the circular progress
- [ ] 4. Show a warning alert banner when today's steps are below the goal

## Review
<!-- filled in after completion -->
